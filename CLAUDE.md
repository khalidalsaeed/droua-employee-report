# ذروة الصعود — دليل المستودع

## تسمية المشاريع — لا تُخلط أبدًا

| الاسم | نطاقه |
|---|---|
| **تاسك أجير** | `sheet_amount` · مطابقة مسير أجير · إيصالات تحويل الموظفين · فاتورة ضمان · المطابقة الآلية والملاحظات |
| **تاسك ذروة** | مراجعة رواتب شركة ذروة |

الاسمان مشروعان منفصلان. لا يُخلطان في تقرير ولا قرار ولا فرع. وكل شيء
في `lib/payroll/` و`payroll-*.html` و`scripts/*payroll*` و`scripts/*jisr*`
يخصّ **تاسك أجير** حتى يُقال غير ذلك صريحًا.

## تاسك أجير — الحالة والفرضية المعلَّقة

| المرحلة | الحالة |
|---|---|
| 1 — `sheet_amount` | ✅ منفَّذة على Production · العشرة مزامَنون |
| 2.0 — قياس `pdf-lib` | ✅ مناسب: 0.80 MB مضغوطًا · اقتطاع صفحة ~2 ms |
| 2.1 — المخطّط | ✅ معتمد · لا أعمدة إضافية على `payroll_transfer_proofs` |
| 2.2 — المحلّل الفردي | ✅ مكتملة · validated على عشرة إيصالات حقيقية |
| 2.2b — تقسيم المجمّع | ✅ `beneficiary_identifier_boundary` · validated على مجمّع حقيقي 10/10 |
| 2.3 فما بعد | ⛔ **لم تبدأ** |

### قاعدة تقسيم المجمّع — `beneficiary_identifier_boundary`

مُختبَرة على مجمّع حقيقي من عشرة إيصالات: **10/10 · صفر يتيمة · صفر
false split**، وكل إيصال يطابق مصدره المفرد في المعرّف والمبلغ والمرجع
و`receipt_hash`.

```
صفحة تحمل معرّف مستفيد (IBAN أو حساب في نطاق «إلى») = بداية إيصال
صفحة بلا معرّف بعد إيصال بدأ                        = تكملته
صفحة بلا معرّف ولم يبدأ إيصال                        = يتيمة، تُبلَّغ
لا معرّف في المستند كلّه                             = إيصال واحد
```

**الحدُّ «وجود» المعرّف لا «تغيّره»** — وهذا فرقٌ جوهري: موظف قد يتلقّى
تحويلين في الشهر نفسه (split payment، حالة مشروعة معتمدة)، وإيصالاهما
المتتاليان يحملان المعرّف نفسه. فقاعدة «التغيّر» تدمجهما وتُخفي
أحدهما — وذاك أسوأ من false split لأنه **يُنقص مبلغًا بصمت**.

وصفحة التكملة تُعرَف بأنها **لا تحمل معرّفًا** أصلًا، فلا تلتبس بإيصال
ثانٍ. وكل إيصال يحمل معرّف سابقه يُوسَم `sameIdentifierAsPrevious`
فيراه الإنسان ولا يُخمَّن له معنى.

⚠️ **حدٌّ مُعلَن:** لم نرَ إيصالًا حقيقيًا يمتدّ صفحتين. فلو كرّر مستندٌ
المعرّف في صفحة التكملة، قسمَ هذا الحدُّ الإيصال إلى اثنين — والوسم هو
ما يُظهر ذلك.

والقاعدة السابقة `splitByNewIban` تبقى مُصدَّرة ومُختبرة: فشلها على
المجمّع الحقيقي (إيصالان من عشرة) توثيقٌ حيّ لسبب تغييرها.

### ما أثبتته المستندات الحقيقية

- إيصال ANB يُخرج **تنسيقين** حسب نوع التحويل: إلى بنك آخر (IBAN مقطوع
  على عنصرين، `Δx=74 Δy=15`) وداخل البنك (رقم حساب، والعملة **قبل**
  المبلغ).
- الوسوم العربية تخرج بصور العرض (`ﻣﻦ` لا `من`) — `NFKC` لازم قبل أي
  مطابقة وسم.
- الوسم قد يقع في **سطر عناوين أعمدة** والقيم في السطر التالي. فالنمط
  أساس والوسم تعزيز — وعكسُ الترتيب جعل المراجع العشرة متطابقة وبدت
  العملية ناجحة.
- تمييز حساب المستفيد من حساب المُرسِل **بالموضع لا بالطول**: نطاقات
  «من · إلى · التفاصيل» في `splitSections`.

## سياسة البيانات: لا بيان هوية حقيقي في Git

ممنوع في المستودع: رقم إقامة · IBAN · رقم حساب بنكي · أي مبلغ يربط
براتب شخص معيّن · أي مستند أصلي (كشف رواتب، فاتورة، إيصال، تصريح).

Git لا ينسى: ملفٌّ يُودَع اليوم يبقى في التاريخ ولكل من يستنسخ المستودع،
وحذفه من `HEAD` وحده لا يمحوه.

**الحاجز:** `test/no-real-data.test.js` يمسح كل ملفّ نصّي ويرفض هذه
الأنماط، ويرفض أي PDF غير مُعلَن في قائمته. فالسياسة محروسة بالكود لا
بالانضباط.

**العيّنات تُبنى بالكود لا تُنسخ:** `test/fixtures/payroll-sheet.js`
يبني كشف رواتب ببنية الكشف الحقيقي حرفيًا — اثنا عشر عمودًا نقديًا عند
الإحداثيات نفسها، ورقم جسر آخر عنصر أفقيًا، واسمٌ مبعثر بترتيب معكوس،
وصفّ إجماليات بلا رقم موظف — وبيانات هوية ومبالغ مُختلقة. أرقام ضمان
فيها `9xx` (الحقيقية `5xx`) وأرقام جسر `11–20` (الحقيقية `49–85`)، فلا
يمكن لصفٍّ منها أن يشير إلى موظف حقيقي حتى بالخطأ.

بعد أي تعديل على المولّد: `node test/fixtures/payroll-sheet.js --write`

**دَينٌ قائم قبل السياسة** (مُعلَن في الحاجز، حذفه معلَّق على فحص أن لا
صفّ في القاعدة يشير إليه): `payroll/2026-07.pdf` و`permits/TQ62117*.pdf`.

## الترقيمان — أصل عطلٍ كلّفنا وقتًا

| الترقيم | المدى | مكانه |
|---|---|---|
| **رقم ضمان** = `الرقم الوظيفي` | `5xx` | `employees.eid` — مفتاح الربط عبر أربعة جداول |
| **رقم جسر** | مرتبتان | `employees.data->>'رقم جسر'` |

أرقام كشف الرواتب أرقام **جسر** لا الرقم الوظيفي. أي شيفرة تبحث برقم
الكشف في `الرقم الوظيفي` تفشل على العشرة كلهم — وقد حدث.

الربط في `lib/payroll/sheetLink.js` **وحده**، بـ«رقم جسر» فقط. والاسم
لا يُنشئ رابطًا أبدًا (مطابقٌ مضبوط على كشف يوليو حسم ٣ من ١٠ على
أغسطس)، والمبلغ لا يُعرّف موظفًا إطلاقًا.

## قيود Vercel Hobby

- **الدوال الخادمة = 2** (`api/app.js` + `api/cron/check-expirations.js`). لا تُزَد.
- لا `maxDuration` في `vercel.json` — السقف ١٠ ثوانٍ.
- حدّ الحزمة 250MB و`node_modules` 141M أصلًا (`pdfjs-dist` وحده 35M).
  أي اعتمادية جديدة تحتاج قياس حجم البناء أولًا.

## أنماط لازمة

- `sql` قالب موسوم. `sql.unsafe()` تُرجع وصفًا **لا تنفّذ** —
  `await sql.unsafe(x)` يخرج بنجاح وقد أصدر صفر طلبات. التنفيذ
  بـ`sql.query(text)`.
- المال `numeric(12,2)`، والمقارنة **بالهللات الصحيحة** لا بالريالات
  العائمة: السماحية بين الإيصال والمسير صفرٌ تامّ.
- **`Number(null) === 0` و`Number("") === 0`** — أوقعنا هذا الفخّ ثلاث
  مرّات: في `sheet_amount`، وفي `toHalalas`، وفي إحداثي عنصر نصّي. فقيمةٌ
  مفقودة تعبر `Number.isFinite` وتُخزَّن صفرًا يبدو بيانًا صحيحًا. الفحص
  يشترط **النوع** لا مجرّد قابلية التحويل: `typeof x !== "number"` أو نمط
  صريح للرقم. و`NaN` أخطر: كل مقارنة معه `false`، فيمرّ من كل حاجز.
- استخراجٌ يلصق أجزاءً من مواضع متباعدة **يخترع** بيانًا لم يُطبع —
  وحسابٌ مخترع قد يقابل موظفًا حقيقيًا. الحاجز على النتيجة لا على
  الطريقة: `ibanScan` في `lib/payroll/receiptFields.js`.
- `NULL` في `sheet_amount` معناه «لم تُزامَن بعد» **لا صفر**.
- التطبيع العربي على مستوى الكلمة: `\b` لا يحدّ الكلمات العربية.
- كل حاجز عدم تكرار في القاعدة: `ON CONFLICT DO NOTHING RETURNING id`
  للإدراج، و`WHERE ... IS DISTINCT FROM` للتحديث.

## الاختبارات

`npm test` — والمطلوب أن يُثبَت لكل ضمانة أن اختبارها **يعضّ**: يُعطَّل
السلوك ويُتحقَّق أن الاختبار يسقط. اختبارٌ يمرّ على شيفرة معطوبة أسوأ من
غيابه.

ومنطق التقارير يُختبر كما يُختبر المنطق: عطلٌ في العرض يمحو أثر عملية
سليمة، وقد انهار تقرير الـbackfill يومًا بعد عمل صحيح تمامًا.
